/* FIG MODEL EXAMPLE
   FOR PARSER TESTING PORPOUSE 
   Raúl Monti 2015
    */



MODULE modone





MODULE modone

LABELS
    a:input; 
    b:output;

CLOCKS
    clockone:Normal(1,1); clocktwo:Exponential(3);
    clockfour: Uniform(6,2); a:Normal(2,-2);

VARS
    bool v = false;
    int a = 2;
    int c = false || true;
    bool b = false;
    int t = 0;

TRANS

    [b?] a > 0 && t > 0 && a + t + 4 == 9 -> ;
    /* Some transition that * represents / something. */
    [a] c == 2 && true && false != (3.653  >= 3/9) : a  -> a = 1, c = false ;
    [b?] a == 4 + false > c : clockone 
     ->  a = 4+2, b = a||b :clockone, clockfour;
    [c!] -(3+10/2) : c -> ;






PROPERTY prop1 :  true;





MODULE modthree

CLOCKS
    clockthree: /* This clock has exponential delay. */
                Exponential(2);
    clockfour :Normal(-1,-2);

TRANS

    [] : c1 -> ;
    [b] : c1 -> : c2;

PROPERTY prop1 : 3/2 > modthree.clockthree +1 && false;






MODULE modfour

VARS

    float c [2..3] = 2;
    int a [2..5] = false || 8 > 2;
    float b = 1.2;
    float cc;
    int d;
    float dd;

TRANS


PROPERTY prop1 : false ;
